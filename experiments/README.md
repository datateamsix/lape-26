# Experiments

Each experiment should record:

- Git commit
- Mapping ID and checksum
- Dataset manifest versions
- Objective and metric versions
- Random seed
- Search parameters
- Runtime environment
- Output metrics and artifacts

Committed experiment definitions belong in `params/`. Large run outputs belong in `runs/` and are ignored until a formal artifact strategy is selected.
